/**
 * Default cache time-to-live in milliseconds
 * Disabled tests are cached per worker to reduce API calls
 */
export const DEFAULT_CACHE_TTL_MS = 60_000; // 1 minute

/**
 * Default API request timeout in milliseconds
 */
export const DEFAULT_API_TIMEOUT_MS = 5_000; // 5 seconds
